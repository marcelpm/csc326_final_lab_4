"""

Back end testing



	Instance up and running:

		check that the instance is started correctly and ip is in fact returned (one which works)

		check that can ssh in

		check that files are ALL uploaded

		check that all packages are installed

		# pem files required, instance id if needed

	Crawler:

		check that the crawler is started without error

		check that the crawler is running

		check that the database is built and can return lists

		# here use the self stored html files for testing

		MUST TEST INDIVIDUAL CRAWLER FUNCTIONS


Front End Testing:

	Page_access:

		No dead ends




	Querying:

		required that all test cases are handled for strings

			short/long strings

			acceptable chars

			empty string

			check time metrics

			


"""